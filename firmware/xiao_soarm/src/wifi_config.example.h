#pragma once

// Copy this file to wifi_config.h and fill in your local network settings.
// wifi_config.h is ignored by Git and must never be committed publicly.
const char* WIFI_SSID = "YOUR_2G4_WIFI_SSID";
const char* WIFI_PASS = "YOUR_WIFI_PASSWORD";
const char* AGENT_IP = "YOUR_COMPUTER_IP";
